import { Text, View } from 'react-native';

export default function ProfileScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-z-bg">
      <Text className="text-lg font-semibold text-z-text">Profile</Text>
    </View>
  );
}
