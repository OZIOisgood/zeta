@AGENTS.md

## Claude Code Compatibility

- Project skills live in `.agents/skills`; this repo exposes them to Claude Code through `.claude/skills`.
- If the skill list looks stale, run `/skills` or restart Claude Code after pulling changes.
- Use `.agents/plans` and `.agents/reports` for new records. Historical task context is summarized in `.agents/reports/20260604170000_legacy_task_baseline.md`.
