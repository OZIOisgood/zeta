# Agent Workspace

Shared agent artifacts for this repository:

- `skills/` - repo-scoped Agent Skills with `SKILL.md` entrypoints.
- `plans/` - new implementation plans and design notes.
- `reports/` - completion reports, audits, and investigation summaries.

Codex reads `.agents/skills` directly. Claude Code reads the same skills through `.claude/skills`.
