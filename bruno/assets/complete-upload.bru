meta {
  name: Complete Upload
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/assets/{{assetId}}/complete
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body.status).to.equal("ok");
}
