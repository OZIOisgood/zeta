meta {
  name: List Assets
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/assets
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body).to.be.an("array");
}
