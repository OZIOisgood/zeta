meta {
  name: Get My Timezone
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/coaching/timezone
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminBearerToken}}
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body.timezone).to.be.a("string");
}
