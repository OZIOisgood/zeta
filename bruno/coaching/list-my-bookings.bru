meta {
  name: List My Bookings
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/coaching/bookings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminBearerToken}}
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body).to.be.an("array");
}
