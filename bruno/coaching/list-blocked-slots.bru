meta {
  name: List Blocked Slots
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/groups/{{groupId}}/coaching/blocked-slots
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminBearerToken}}
}

tests {
  expect(res.status).to.equal(200);
  expect(res.body).to.be.an("array");
}
